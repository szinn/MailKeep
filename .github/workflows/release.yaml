---
name: "Release: MailKeep"

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version to build
        default: "latest"
        required: true
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build:
    name: Build MailKeep
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: Setup Tools
        shell: bash
        run: |
          sudo apt-get -qq update && \
          sudo apt-get -qq install --no-install-recommends -y curl unzip gcc g++ git

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1

      - name: Login to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Setup workflow Variables
        id: vars
        shell: bash
        run: |
          if test "$GITHUB_EVENT_NAME" == "workflow_dispatch"; then
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
            echo "tag_version=mailkeep:${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            echo "version=$(echo $GITHUB_REF | cut -d / -f 3)" >> "$GITHUB_OUTPUT"
            echo "tag_version=mailkeep:$(echo $GITHUB_REF | cut -d / -f 3)" >> "$GITHUB_OUTPUT"
          fi

      # - name: Setup QEMU
      #   uses: docker/setup-qemu-action@68827325e0b33c7199eb31dd4e31fbe9023e06e3 # v3.0.0

      - name: Setup Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0

      - name: Build container image
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7
        with:
          build-args: |-
            VERSION=${{ steps.vars.outputs.version }}
          context: .
          platforms: linux/amd64 # load does not support muti-arch https://github.com/docker/buildx/issues/290
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ steps.vars.outputs.tag_version }}
            ghcr.io/${{ github.repository_owner }}/mailkeep:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract release notes
        id: release_notes
        if: github.event_name == 'push'
        shell: bash
        run: |
          BARE_VERSION="$(echo '${{ steps.vars.outputs.version }}' | sed 's/^v//')"
          awk -v ver="$BARE_VERSION" '
            BEGIN { pat = "^## \\[" ver "\\]" }
            $0 ~ pat { found = 1; next }
            /^## \[/ { if (found) exit }
            found { print }
          ' CHANGELOG.md > release_notes.md

      - name: Create GitHub Release
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          gh release create "${{ steps.vars.outputs.version }}" \
            --title "MailKeep ${{ steps.vars.outputs.version }}" \
            --notes-file release_notes.md
